<!-- Add these dependencies to the target Spring Boot application's pom.xml if they are not already present. -->

<!--
    MyBatis Spring Boot Starter (replaces spring-boot-starter-jdbc).
    Pick the version that matches your Spring Boot version:
      - Spring Boot 3.x  →  mybatis-spring-boot-starter 3.x
      - Spring Boot 2.x  →  mybatis-spring-boot-starter 2.x
-->
<dependency>
    <groupId>org.mybatis.spring.boot</groupId>
    <artifactId>mybatis-spring-boot-starter</artifactId>
    <version>3.0.3</version>
</dependency>

<!-- PostgreSQL JDBC driver -->
<dependency>
    <groupId>org.postgresql</groupId>
    <artifactId>postgresql</artifactId>
    <scope>runtime</scope>
</dependency>

<!-- Spring Web (REST controller) -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>

<!-- Bean Validation (request body @Valid) -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-validation</artifactId>
</dependency>
